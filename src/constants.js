// Add entries here as new kinds are introduced.
// Consumers fall back to 'gray' for unknown kinds.
export const KIND_COLORS = {
  class: 'red',
  interface: 'orange',
  mixin: 'plum',
  namespace: 'green',
  function: 'green',
  object: 'cyan',
  constant: 'blue',
  property: 'blue',
  typedef: 'orange',
  event: 'plum',
};
